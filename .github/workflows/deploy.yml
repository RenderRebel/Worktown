name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest httpx

      # Uncomment below to run tests if you have pytest configured
      # - name: Run Pytest
      #   run: pytest

  build-and-deploy:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test # Only runs if tests pass
    if: github.ref == 'refs/heads/main' # Only deploy from main branch
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker Image
        run: |
          docker build -t service-marketplace-api:latest .

      # =========================================================
      # DEPLOYMENT EXAMPLES (Uncomment and configure as needed)
      # =========================================================

      # --- Option 1: Push to Docker Hub ---
      # - name: Log in to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #
      # - name: Push Docker Image
      #   run: |
      #     docker tag service-marketplace-api:latest ${{ secrets.DOCKER_USERNAME }}/service-marketplace-api:latest
      #     docker push ${{ secrets.DOCKER_USERNAME }}/service-marketplace-api:latest

      # --- Option 2: Deploy to Render (Example using Webhook) ---
      # - name: Trigger Render Deployment
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
